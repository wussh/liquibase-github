# ── Ignore local credential overrides ──
.env
.env.*
!.env.example
liquibase/liquibase.local.properties

# ── Docker volumes ──
mysql_data/

# ── OS junk ──
.DS_Store
Thumbs.db

# ── IDE ──
.idea/
.vscode/
*.iml
